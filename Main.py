import streamlit as st
from streamlit_extras.app_logo import add_logo
from streamlit_extras.card import card
import base64
from utils import ReadData
import matplotlib.pyplot as plt


if __name__ == '__main__':
    # st.title('200-6数据分析器')
    # 标题居中
    # phone_num = '15528932507'
    # password = '123456'
    # date = '2025-01-07'
    # mac = 'Sck-M1-7cdfa1b89d04'

    # df = ReadData.ReadData_Day(beeId='86200001289', mac=mac, time=date, PhoneNum=phone_num, password=password, DataType='P')
    # figure,ax = plt.subplots(figsize=(6,6))
    # df.plot(x='Time', y='P', ax=ax)
    # time_list = [i[-10:] for i in df['Time']]
    # ax.set_xticks(range(0, len(time_list), 20))
    # ax.set_xticklabels(labels=time_list[::20], rotation=45)
    # st.write(figure)

    # 放一个简单的card
    pass




















